По умолчанию стоит сборщик мусора G1 со следующими характеристиками:

Committed memory: 253,952 kbytes
Maximum heap size:  4,018,176 kbytes

Если я правильно понял, G1  стоит по умолчанию с 9 JDK. (У меня стоит OpenJDK 64 bit 11.0.4 для Ubuntu)

После запуска количество потоков остаётся постоянным : 13.
Количество классов остается постоянным также : 2337
Куча пополняется с течением времени в основном только из за подключения Jconsole или Visual VM программ. Выполнение любых операций в программе не вызывает значительного ускорения заполнения кучи после выполнения любой операции (добавления, поиска редактирования). 
За 40  минут наблюдения освобождалась 6 раз young generation
old generation сработала при ручном запуске и один раз самостоятельно.

Visual VM даёт возможность посмотреть подробно heapdump: какие именно объекты находятся в куче, сколько они занимают места и какие присвоены значения их полям. 

Общая загруженность кучи, процент использования процессора, потоки, какие потоки загружают процессор и память. 

При запуске теста программы в бесконечном цикле через некоторое время появилась ошибка по памяти. 
График использования памяти в куче: куча начала чаще срабатывать на очищение, размер кучи вырос, после ошибки подключение к куче оборвалось. 
При этом куча останавливала только поток RMI TCP Connection. 

